# Full‑Stack Task Manager

A production‑ready template with:
- **Backend**: Node.js 20, Express, TypeScript, TypeORM, JWT auth
- **Frontend**: React 18, TypeScript, Vite, React‑Query
- Dockerised services (Postgres, app, UI)

See the `backend/` and `frontend/` READMEs for setup instructions.
