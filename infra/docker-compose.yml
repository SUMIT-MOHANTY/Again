version: '3.8'
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    volumes:
      - ./certs:/app/certs
    ports:
      - "8443:8443"
    environment:
      - CERT_PATH=/app/certs/placeholder.crt
      - KEY_PATH=/app/certs/placeholder.key
